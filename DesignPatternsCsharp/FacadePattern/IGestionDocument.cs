namespace FacadePattern
{
    // Le composant de gestion de documents constitue une simulation de base de documents
    public interface IGestionDocument
    {
        string Document(int index);
    }
}